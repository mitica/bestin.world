---
import type { InsightInfo, TopicInfo } from "../content/common/types";

const { item, topics } = Astro.props as {
  item: InsightInfo;
  topics: TopicInfo[];
};
---

<h4>
  {item.emoji}
  {item.title}
</h4>
<p class="text-secondary">
  {item.description}
</p>
<p class="flex flex-wrap gap-2">
  {
    topics.map((topic) => (
      <a class="a inline-block" href={`/${topic.id}/`}>
        {topic.name}
      </a>
    ))
  }
</p>
